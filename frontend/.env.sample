# Backend API base URL — no trailing slash
VITE_API_BASE_URL=http://localhost:5000

# Optional: wire to your own error ingest endpoint (Sentry proxy, /api/errors, etc.)
# VITE_ERROR_REPORTING_ENDPOINT=https://your-error-ingest.example.com/api/errors
